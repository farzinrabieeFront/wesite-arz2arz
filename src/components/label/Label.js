import React from 'react'

const Label = ({text, color}) => {
    return (
        <>
            <span className={`${Styles.label} ${Styles.greenLabel} `}>آموزش</span>
        </>
    )
}

export default Label
